# Navigate to your repo
cd Badges

# Replace the workflow file with the fixed version
# (copy the content from the fixed file above)
nano .github/workflows/daily-motivation.yml

# Then save and push
git add .github/workflows/daily-motivation.yml
git commit -m "Fix GitHub Actions workflow"
git push origin main
